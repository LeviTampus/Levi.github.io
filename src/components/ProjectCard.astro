---
import type { CollectionEntry } from 'astro:content';
import Button from './Button.astro';

interface Props {
  project: CollectionEntry<'projects'>;
}

const { project } = Astro.props;
const { title, type, tagline, technologies, links, featured, wip, wipNote } = project.data;

const badge: Record<string, string> = {
  professional: 'Professional',
  personal: 'Personal',
  learning: 'Learning',
};
---
<article class={`card${featured ? ' featured' : ''}`}>
  <header>
    <p class="tags">
      <span class="tag badge">{badge[type]}</span>
      {featured && <span class="tag featured-tag">Featured</span>}
      {wip && <span class="tag wip-tag">Work in progress</span>}
    </p>
    <h2>{title}</h2>
    <p class="tagline">{tagline}</p>
  </header>
  <div class="prose">
    <slot />
  </div>
  {wipNote && <p class="wip-note">{wipNote}</p>}
  {technologies.length > 0 && (
    <ul class="tech" aria-label="Technologies">
      {technologies.map((t) => (
        <li class="tag">{t}</li>
      ))}
    </ul>
  )}
  {(links.demo || links.repo || links.video) && (
    <p class="links">
      {links.demo && <Button href={links.demo} external>Live demo</Button>}
      {links.repo && (
        <a href={links.repo} target="_blank" rel="noopener noreferrer">Repository</a>
      )}
      {links.video && (
        <a href={links.video} target="_blank" rel="noopener noreferrer">Video</a>
      )}
    </p>
  )}
</article>

<style>
  .card {
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--surface);
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .card.featured {
    border-color: var(--accent);
  }

  .card h2 {
    font-size: 1.17em;
    margin: 0.25rem 0 0.5rem;
  }

  .tagline {
    margin: 0;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin: 0;
  }

  .badge {
    display: inline-block;
    color: var(--muted);
    background: var(--surface-tint);
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.15rem 0.7rem;
    margin: 0;
  }

  .featured-tag {
    display: inline-block;
    color: var(--accent-strong);
    background: var(--accent-soft);
    border-radius: 999px;
    padding: 0.15rem 0.7rem;
    margin: 0;
  }

  .wip-tag {
    display: inline-block;
    color: var(--muted);
    background: var(--surface-tint);
    border: 1px dashed var(--rule);
    border-radius: 999px;
    padding: 0.15rem 0.7rem;
    margin: 0;
    font-weight: 600;
    font-size: 0.78rem;
  }

  .prose :global(h3) {
    font-size: 1rem;
    margin: 1.25rem 0 0.5rem;
  }

  .prose :global(h4) {
    font-size: 0.95rem;
    margin: 1rem 0 0.4rem;
  }

  .wip-note {
    color: var(--muted);
    font-size: 0.9rem;
    border-left: 3px solid var(--rule);
    padding-left: 0.75rem;
    margin: 1rem 0 0;
  }

  .tech {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
    margin: 1rem 0 0;
    padding: 0;
  }

  .tech li {
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.2rem 0.65rem;
    color: var(--muted);
    font-weight: 500;
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: center;
    margin: 1.25rem 0 0;
  }
</style>
